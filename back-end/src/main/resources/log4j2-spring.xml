<?xml version="1.0" encoding="UTF-8"?>
<!--TODO convert to yaml-->
<Configuration>
    <Properties>
        <!--        TODO Must change properties: project-name, package -->
        <Property name="project-name">spring-learning</Property>
        <Property name="package">com.example</Property>

        <Property name="log-pattern">[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n</Property>
        <Property name="folder-log">./logs</Property>
    </Properties>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log-pattern}"/>
        </Console>
        <RollingFile name="RollingFile"
                     fileName="${folder-log}/${project-name}.log"
                     filePattern="${folder-log}/$${date:yyyy-MM}/%d{yyyy-MM-dd}-${project-name}-%i.log.gz">
            <PatternLayout pattern="${log-pattern}"/>
            <Policies>
                <!-- rollover on startup, daily and when the file reaches 10 MegaBytes -->
                <OnStartupTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <TimeBasedTriggeringPolicy/>
            </Policies>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- LOG everything at INFO level -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>

        <!-- LOG "com.baeldung*" at TRACE level -->
        <Logger name="${package}" level="trace"/>
    </Loggers>

</Configuration>